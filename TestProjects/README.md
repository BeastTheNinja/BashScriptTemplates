# Test Project Output

This folder contains a generated React + Vite project created by the setup script.

It is used to test that the script runs correctly and creates the expected files, folders, and dependencies.

If you want to make the setup script better, this folder is the place to verify the result before changing the template itself.
